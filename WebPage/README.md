Wanted this to work with the native(almost) python code so used [brython](https://pypi.org/project/Bython/) to be able to run python inside front end JS.

Uses:
  - html
  - css styling, some bootstrap, some of my classes
  - Brython
